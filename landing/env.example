# Supabase Configuration
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Meta Pixel (client-side)
# Get your Pixel ID from: https://business.facebook.com/events_manager > Data Sources > Pixel
NEXT_PUBLIC_META_PIXEL_ID=

# Google Analytics (optional - leave empty to disable)
NEXT_PUBLIC_GA_ID=

# Copy this file to .env.local and fill in your values

# ============================================
# IMPORTANT: Supabase Edge Function Secrets
# ============================================
# The following secrets must be set in Supabase (NOT in Vercel):
# 
# 1. META_PIXEL_ID - Same as NEXT_PUBLIC_META_PIXEL_ID above
# 2. META_CONVERSIONS_API_TOKEN - Your Conversions API access token
#    Get it from: https://business.facebook.com/events_manager
#    Select your Pixel > Settings > Conversions API > Generate Access Token
#
# To set these secrets, run:
#   supabase secrets set META_PIXEL_ID=your_pixel_id
#   supabase secrets set META_CONVERSIONS_API_TOKEN=your_token
#
# Or set them in the Supabase Dashboard:
#   Project Settings > Edge Functions > Secrets
#
# ============================================
# Testing Meta Conversions API
# ============================================
# To test events without affecting real data:
# 1. Go to Meta Events Manager > Your Pixel > Test Events
# 2. Copy the test event code (e.g., TEST12345)
# 3. Pass it in the API call: { "test_event_code": "TEST12345" }
# 4. Events will appear in the Test Events tab only
